# `tests/`

**Owner:** Everyone — you write the tests for your own component

Automated tests. These run on every push once CI is set up.

## Files that belong here

| File | What it does |
|:--|:--|
| `test_<component>.py` | One test file per component, named after what it tests. |

## Contract

A component without tests is not finished.

---

*Delete this README once the folder has real files in it.*
