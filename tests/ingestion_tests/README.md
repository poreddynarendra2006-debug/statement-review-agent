# `tests/ingestion_tests/`

**Owner:** Data Ingestion

Upload the files from your `tests/` folder:

`__init__.py`, `test_cleaner.py`, `test_mapper.py`, `test_multi_datasets.py`, `test_new_requirements.py`, `test_pipeline.py`, `test_reader.py`, `test_validator.py`

**Before uploading:** in every test file, replace `src.ingestion` with `extraction`. In this repository your code lives in `extraction/`, not `src/ingestion/`.

Upload into this folder only, never into `tests/` directly.
