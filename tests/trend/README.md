# `tests/trend/`

**Owner:** Trend

| File | |
|:--|:--|
| `conftest.py` | Trend's own fixtures - applies to the tests in this folder only |
| `test_deviation.py`, `test_edge_cases.py`, `test_forecasting.py`, `test_mapping.py`, `test_trend.py`, `test_yoy.py` | Trend's tests |

Upload into this folder only, never into `tests/` directly. The code lives in `analysis/` and `config/`.
