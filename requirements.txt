# Core
pandas>=2.0.0
numpy>=1.24.0

# File ingestion
openpyxl>=3.1.2

# Reading back the PDF reports we generate, so the tests can check them.
# PDF *ingestion* is out of scope (team decision, 12 Sep 2026).
pypdf>=4.2.0

# Machine learning
scikit-learn>=1.3.0

# HTTP API, file upload, and serving the reviewer's screens
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Reporting
reportlab>=4.1.0

# Actual-vs-forecast charts from the trend agent. It was imported but never
# declared, so it worked on our own machines and was missing from the container
# - the first CI run caught it.
matplotlib>=3.8.0

# Loading a local .env file
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
